name: C/C++ CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v1
    - name: configure
      run: |
        set +x
        ls
        pwd
        git submodule update --init --recursive
        cmake .
        cmake --build
      shell: bash
